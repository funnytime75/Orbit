{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for Orbit windows",
  "windows": ["main", "wheel"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:default"
  ]
}
